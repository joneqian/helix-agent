"""Knowledge-base ingestion pipeline — Stream J.5.

Parsing (``parsing``), chunking (``chunking``) and the async ingestion
runner (``ingestion``) that turn an uploaded document into searchable
``knowledge_chunk`` rows. See ``docs/streams/STREAM-J-DESIGN.md`` § 12.
"""
