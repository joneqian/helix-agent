__pycache__/
*.pyc
.venv/
.env
*.egg-info/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
data/
.DS_Store
.claude/

# Stream C.2 — dev mTLS PKI generated by tools/dev-certs/generate.py.
# Never commit private keys (even dev) so secret-scanners stay clean.
infra/dev-certs/

# Stream H — Admin UI demo / future admin-ui (apps/admin-ui*/) — Node / Vite 输出
node_modules/
*.local
.vite/
apps/*/dist/
apps/*/.turbo/
apps/*/*.tsbuildinfo

# Stream H.1b PR 4 — Storybook + Playwright artefacts
apps/admin-ui/storybook-static/
apps/admin-ui/playwright-report/
apps/admin-ui/test-results/

# Local docker-compose overrides (per-machine port remaps etc.)
docker-compose.override.yml
infra/docker-compose.override.yml

# Stream CM-N5 — benchmark dataset cache (not vendored: license + size)
tools/eval/datasets/.longmem_cache/
