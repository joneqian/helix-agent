# Helix "office" sandbox image — Python office libraries (OFFICE-ADR-1).
#
# Top-level pins only; transitive deps (numpy, pdfminer.six, lxml, …) are
# resolved by pip from manylinux wheels — no compiler in the image. Keep
# this list narrow: every library here is import-available to office agents
# and is part of the sandbox's trusted surface.
pandas==2.2.3          # tabular data / xlsx+csv analysis
openpyxl==3.1.5        # .xlsx read/write
python-docx==1.1.2     # .docx read/write
python-pptx==1.0.2     # .pptx read/write
pypdf==6.10.2           # PDF read / merge / split
pdfplumber==0.11.4     # PDF text + table extraction
Pillow==11.0.0         # image read/write/convert
matplotlib==3.9.2      # charts (CJK rendering via fonts-noto-cjk)
