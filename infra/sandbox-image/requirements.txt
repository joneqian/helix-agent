# Helix "office" sandbox image — Python office libraries (OFFICE-ADR-1).
#
# Top-level pins only; transitive deps (numpy, pdfminer.six, lxml, …) are
# resolved by pip from manylinux wheels — no compiler in the image. Keep
# this list narrow: every library here is import-available to office agents
# and is part of the sandbox's trusted surface.
pandas==2.2.3          # tabular data / xlsx+csv analysis
openpyxl==3.1.5        # .xlsx read/write
python-docx==1.1.2     # .docx read/write
python-pptx==1.0.2     # .pptx read/write
pypdf==6.13.3           # PDF read / merge / split
pdfplumber==0.11.4     # PDF text + table extraction
# PDF *generation* (the read libs above can't author a PDF). markdown→HTML→PDF
# is the de-facto report path; weasyprint renders HTML+CSS → PDF and picks up
# the baked noto-cjk fonts (needs the libpango/cairo apt libs added in the
# Dockerfile). This closes the gap that made md→PDF tasks thrash on the missing
# installer.
markdown==3.8.1          # Markdown → HTML
weasyprint==68.0       # HTML+CSS → PDF (report generation)
# Pin pydyf: weasyprint 62.3 declares ``pydyf>=0.10.0`` with no upper bound, but
# pydyf 0.12.x reworked the Stream API (``super().transform`` AttributeError at
# write_pdf time). 0.11.0 is the newest pydyf compatible with this weasyprint.
pydyf==0.11.0          # weasyprint PDF backend (API-pinned, see above)
Pillow==12.2.0         # image read/write/convert
matplotlib==3.9.2      # charts (CJK rendering via fonts-noto-cjk)
# route ① §5.4 — Python bridges to the new apt binaries (soffice/poppler/ffmpeg).
pdf2image==1.17.0      # PDF→image; wraps the poppler-utils binaries
imageio==2.37.3        # gif/video frames (slack-gif-creator)
imageio-ffmpeg==0.6.0  # imageio's ffmpeg bridge (binary from apt ffmpeg)
defusedxml==0.7.1      # office unpack/clean scripts import it directly
